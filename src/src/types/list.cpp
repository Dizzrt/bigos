/*
 * @Author: Dizzrt
 * @LastEditTime: 2021-12-02 12:13:32
 */

#include "list.h"

// static inline void __list_add(list_head *node, list_head *prev, list_head *next) {
//     // TODO __list_add_valid
//     //  if (!__list_add_valid(node, prev, next))
//     //      return;

//     next->prev=node;
//     node->next=next;
//     node->prev=prev;

// }

// static inline void list_add(list_head *node, list_head *head) { __list_add(node, head, head->next); }
